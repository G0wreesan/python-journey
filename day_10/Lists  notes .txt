Lists are useful data types, as they allow you to write code that works on any number of values 
contained in a single variable. 

you can save a list in a variable and pass it to a function then the values inside the list 
can be accessed via refering to it's numerical value :) 

    1. What is the first index of any list?
        0

    2. If a variable named spam contains ['cat', 'bat', 'rat', 'hat'], 
    what does spam[3] evaluate to? 
    'hat'

    3. If a variable named spam contains ['cat', 'bat', 'rat', 'hat'], 
    what does spam[4] evaluate to?
    -null-   indexError

    4. Do all the values in a Python list need to be of the same data type?
        No 

    5. If a variable named spam contains an empty list, what happens when spam[0] is evaluated?
        Index indexError

    6. In the expression spam[3], is the [3] also a list?
        no, it's an index of the element in the list 

    7. What negative index is equivalent to the index in spam[len(spam) - 1]?
        -1 (always the last item on the list)

    8. What negative index is equivalent to the index in spam[len(spam) - 3]?
        -3 (counts form the end)

    9. If a variable named spam contains a list, 
        what is the difference between the statement del spam[0] and the statement del spam?
        del spam[0] will delete the 1st element in the spam list
        del spam will dleete the entire variable and
        any reference to spam will raise a name error in code 

13. If an expression is using the in and not in operators, what data type does it evaluate to?
    Bool [True or false]

14. If spam contains the list ['cat', 'dog', 'moose'] and Python runs the statement a, b, c = spam, 
    what does the b variable contain?
    'dog'

15. If Python runs the statement a, b, c = 'cat', what does the b variable contain?
    'a'

16. Say spam contains a list value and Python runs the statement for a, b in enumerate(spam):. 
    Describe the data that the a and b variables contain.
    a  = index 
    b = element in the list

17. What does the random.choice() function return?
    one random element from the list of elements 
    +(RANDOM.CHOICE('cat','bat','dog'))
    
18. What does the random.shuffle() function do?
    randomly reorder the elements in the lists 


19. If spam contains the list ['cat', 'dog', 'moose'] and Python runs import random and 
    random.shuffle(spam), what does the expression len(spam) evaluate to?
    3 
    shuffling will only the changes the order the elements lined into not the length of list


Augmented Assignment Operators
Augmented assignment operators are shortcuts for changing the value of a variable based on its current value. 
They exist for the +, -, *, /, and % operators.

Methods 
Methods are just methods are just like functions excepts it's called on a value,or object
object.methods();

short-circuiiting booleans

in python we can run boolean expressions little faster by without checking the right handside of the operators
ths is short circuiiting


Sequence the Data Types 
    not only the list contain a sequence of value infact the String too can be considered as a list of 
    sequential Characters 

    48. List at least two different sequence data types in Python.
    Lists and String
    /tuple /range

    49. Why doesn’t 'Zophie'[1] evaluate to 'Z' if Z is the first character?
    because python uses Zero based indexing 
    'Zophie'[0] --> will evaluated to 'Z'

    50. Why doesn’t 'Zophie'[1] evaluate to 'Z' if Z is the first character?
    this will evaluated to the last character 
    'Zophie'[-1]='e'

    51. What does 'Zophie'[9999] evaluate to?
    will raise and index error 
    index is out of range

References 
    reference are the link  between variable name and an object in memory

    55. Aside from brackets/parentheses, main difference between lists and tuples?
    lists can be changed 
    tuples can not be changed

    56.Code to obtain a list from the tuple ('cat', 'dog')
    list(('cat','dog'))

    57.Code to obtain a tuple from the list ['cat', 'dog']
    tuple(['cat', 'dog'])

    58.What happens if you run this code?
    spam = ('cat', 'dog', 'moose')
    spam[2] = 'cow'

    type error because tuples can not be changed

    59. In Python, variables never contain values. What do they contain?
    References to values
    
    60. The = assignment operator never copies values. What does it copy?
    references

    61. How many copies of the list exist here?
    a = ['cat', 'dog', 'moose']
    b = a
    c = a
    only one copy of list because the varibles only store references in memory

    62. How many copies exist here?
    import copy
    a = ['cat', 'dog', 'moose']
    b = copy.copy(a)
    c = copy.copy(a)

    3 separate lists cuz a,b,c each reference different list objects

    Which function copies [['cat', 'dog'], 'moose'] correctly?

    63. Which function copies [['cat', 'dog'], 'moose'] correctly?
    copy.decopy()

    